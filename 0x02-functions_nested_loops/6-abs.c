#include "main.h"

/**
 * _abs - Returns the absolute value of a variable
 * @n: character to test
 * Return:  int - value
 *
 */

int _abs(int n)
{

	if (n > 0)
		return (n);
	else
		return (n * (-1));
}
